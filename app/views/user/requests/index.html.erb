<h1 class="text-2xl font-bold mb-6"><%= t("request.details_title") %></h1>

<% if @requests.any? %>
  <%= render partial: "request_card", collection: @requests, as: :request %>
<% else %>
  <p class="text-gray-600"><%= t("request.no_booking") %></p>
<% end %>
